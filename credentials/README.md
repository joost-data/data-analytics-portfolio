cat > credentials/README.md << 'EOF'
# Credentials (public placeholder)

This folder is intentionally public.

- Do NOT store secrets here (API keys, passwords, tokens).
- If you need private credentials locally, use:
  - credentials.local/ (ignored)
  - or environment variables / a secrets manager.

